name: WireMock Cloud Deploy to Environment

on:
  workflow_dispatch:
    inputs:
      name:
        required: true
        type: string
      image_uri:
        required: true
        type: string
      source_action:
        required: true
        type: string
      source_commit:
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-20.04
    steps:

      - uses: actions/checkout@v3
        with:
          ref: main
          token: ${{ secrets.SET_VERSION_TOKEN }}

      - run: ./set-version.sh ${{ inputs.name }} ${{ inputs.image_uri }} ${{ inputs.source_action }} ${{ inputs.source_commit }}

      - uses: EndBug/add-and-commit@v9
        with:
          add: imagedefinitions.json
          message: Set ${{ inputs.name }} to ${{ inputs.image_uri }}
